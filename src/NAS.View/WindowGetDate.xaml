<Window x:Class="NAS.WindowGetDate"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:resx="clr-namespace:NAS.Resources;assembly=NAS.Resources"
        Title="{Binding Title}" WindowStartupLocation="CenterScreen" Icon="pack://application:,,,/NAS.View;component/Images/Calendar.png" ResizeMode="NoResize" mc:Ignorable="d" d:DesignWidth="300" SizeToContent="WidthAndHeight" d:DesignHeight="300" ShowInTaskbar="False">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>
    <Calendar SelectedDate="{Binding Date}" DisplayDateStart="{Binding StartDate, Mode=OneWay}" DisplayDateEnd="{Binding EndDate, Mode=OneWay}" HorizontalAlignment="Center" Margin="5" VerticalAlignment="Center" MouseDoubleClick="calendar_MouseDoubleClick" />
    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Grid.Row="1" Margin="2">
      <Button Content="{x:Static resx:NASResources.OK}" MinWidth="75" Click="buttonOK_Click" IsDefault="True" />
      <Button Content="{x:Static resx:NASResources.Cancel}" MinWidth="75" Margin="2,0,0,0" Click="buttonCancel_Click" IsCancel="True" />
    </StackPanel>
  </Grid>
</Window>
